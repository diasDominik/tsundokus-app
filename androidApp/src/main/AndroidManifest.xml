<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      The passkey libraries declare minSdk 28, the app supports 26. Android 8 keeps the app and
      keeps signing in with a password: nothing in those libraries is loaded below API 28, because
      every entry point sits behind isPasskeySupported(), and the passkey UI is not shown there.
    -->
    <uses-sdk
        tools:overrideLibrary="io.github.androidpoet.passkeys, io.github.androidpoet.passkeys.compose" />

    <application
        android:name=".TsundokuApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="tsundokus.uk" />
                <data android:host="dev.tsundokus.uk" />

                <data android:pathPrefix="/api/auth/verify" />
                <data android:pathPrefix="/api/auth/reset-password" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="tsundokus" />
                <data android:host="tsundokus.uk" />
                <data android:host="dev.tsundokus.uk" />

                <data android:pathPrefix="/api/auth/verify" />
                <data android:pathPrefix="/api/auth/reset-password" />
            </intent-filter>
        </activity>
    </application>

</manifest>
